<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/agentsFragment"
            android:icon="@drawable/ic_menu_camera"
            android:title="@string/menu_agents" />
        <item
            android:id="@+id/weaponsFragment"
            android:icon="@drawable/ic_menu_gallery"
            android:title="@string/menu_weapons"
            android:action="@+id/weaponsFragment"/>
        <item
            android:id="@+id/mapsFragment"
            android:icon="@drawable/ic_menu_slideshow"
            android:title="@string/menu_maps" />
            android:action="@+id/mapsFragment"/>
    </group>
</menu>